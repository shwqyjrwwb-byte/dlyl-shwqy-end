kind: wasmer.io/App.v0
name: dlyl-shwqy-end
package: .

env:
  - name: NODE_ENV
    value: production
  - name: PORT
    value: "3000"

cli_args:
  - npm
  - start

capabilities:
  inbound_http: true
  outbound_http: true

scaling:
  mode: single_concurrency
